// Set API_URL to your production API before `npm run build`.
// Example: https://api.yourdomain.com/api
// See DEPLOYMENT.md at the repository root.
export const environment = {
  production: true,
  API_URL: 'https://api.yourdomain.com/api',
};
